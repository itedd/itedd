.row
  .large-12.columns
    h2 Nutzer bearbeiten
    = simple_form_for(@user, url: user_admin_path(@user)) do |f|
      = f.input :email
      = f.input :password
      = f.input :password_confirmation
      = f.association :user_group, include_blank: false
      = f.input :approved, as: :boolean
      = f.button :submit, 'Speichern'

.row
  .large-12.columns
    = simple_form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post }) do |f|
      = f.input :email
      = f.button :submit, 'Bestätigungsmail erneut anfordern'

.row
  .large-12.columns
    = render 'devise/shared/links'
